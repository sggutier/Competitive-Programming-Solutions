#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a ;

    scanf("%d", &n);

    while(n--) {
        scanf("%d", &a);
        if(a==1) {
            printf("-1\n");
            return 0;
        }
    }

    printf("1\n");
}
